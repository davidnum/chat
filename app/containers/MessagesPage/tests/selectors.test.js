// import { fromJS } from 'immutable';
// import { makeSelectMessagesPageDomain } from '../selectors';

// const selector = makeSelectMessagesPageDomain();

describe('makeSelectMessagesPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
